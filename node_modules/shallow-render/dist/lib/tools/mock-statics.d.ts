import { CustomError } from '../models/custom-error';
import { TestSetup } from '../models/test-setup';
export declare class InvalidStaticPropertyMockError extends CustomError {
    static checkMockForStaticProperties(stubs: object): void;
    constructor(key: string | symbol);
}
export declare const mockStatics: (setup: TestSetup<any>) => void;
