import { Type } from '@angular/core';
export declare type MockDirective = {
    renderContents: () => void;
    clearContents: () => void;
};
export declare function mockDirective<TDirective extends Type<any>>(directive: TDirective, config?: {
    stubs?: object;
    renderContentsOnInit?: boolean;
}): TDirective;
